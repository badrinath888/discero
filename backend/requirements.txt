fastapi==0.135.0
# Pinned explicitly (normally a transitive dep of fastapi) -- the
# starlette version fastapi==0.115.5 previously pulled in (0.41.3) had
# 9 known CVEs (Host-header/path validation bypass, multipart/form
# resource-exhaustion DoS, and others -- see SECURITY.md). fastapi's
# own constraint on starlette for 0.115.x has no version that clears
# them; 1.3.1 is the smallest starlette version that does, and the
# smallest fastapi major compatible with any 1.x starlette is 0.135.0.
starlette==1.3.1
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.19.1
pydantic[email]==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0
cryptography==50.0.0
python-multipart==0.0.32
psycopg2-binary==2.9.10
anthropic==0.40.0
groq==1.6.0
PyJWT==2.13.0
pwdlib[argon2]==0.3.0
plaid-python==40.0.0
resend==2.34.0
redis==6.4.0

# dev / test
pytest==9.0.3
httpx==0.28.1
